---
import Layout from "../layouts/Layout.astro";
---
<Layout>
  <div class="max-w-5xl mx-auto py-8 px-4">
    <div class="mb-8 border-b border-gray-200 pb-6">
      <a href="/" class="inline-flex items-center text-sm font-medium text-blue-600 hover:text-blue-800 mb-4 transition-colors">
        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
        Retour au menu
      </a>
      <h1 class="text-3xl font-extrabold text-gray-900">Simple Penguin</h1>
      <p class="text-gray-500 mt-2">Affichage brut des caractéristiques des manchots.</p>
    </div>

    <div class="bg-white p-6 rounded-2xl shadow-md border border-gray-100 overflow-x-auto text-black">
      <div id="plot-container" class="min-w-[600px]"></div>
    </div>
  </div>

  <script>
    import * as Plot from "@observablehq/plot";
    import penguins from "../assets/penguins.json"; 

    const plot = Plot.plot({
      grid: true,
      marks: [
        Plot.dot(penguins, {
          x: "culmen_length_mm",
          y: "culmen_depth_mm",
          stroke: "species"
        })
      ]
    });
    
    const div = document.querySelector("#plot-container");
    div.append(plot);
  </script>
</Layout>
---
import Layout from "../layouts/Layout.astro";
---
<Layout>
  <h1 class="text-xl mb-4">Penguin avec filtre</h1>
  
 
  <select name="species" id="species" class="mb-4 border p-2 text-black">
    <option value="">All species</option>
    <option value="Adelie">Adelie</option>
    <option value="Chinstrap">Chinstrap</option>
    <option value="Gentoo">Gentoo</option>
  </select>

  <div id="plot-container"></div>

  <script>
    import * as Plot from "@observablehq/plot";
    import penguins from "../assets/penguins.json";
    const selectSpecies = document.querySelector("#species");
    const divContainer = document.querySelector("#plot-container");

  
    function renderPlot() {
    
      const species = selectSpecies.value;
      
      divContainer.innerHTML = "";

      
      const plot = Plot.plot({
        marks: [
        
          Plot.dot(penguins, {
            x: "culmen_length_mm", 
            y: "culmen_depth_mm", 
            stroke: "species", 
            
            filter: (d) => species === "" || d.species === species
          }),
        ],
      });
      
      divContainer.append(plot);
    };
    selectSpecies.addEventListener("change", renderPlot);

    renderPlot();
  </script>
</Layout>